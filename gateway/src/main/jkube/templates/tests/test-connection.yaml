apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jkube.fullname" . }}-test-connection"
  labels:
    {{- include "jkube.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jkube.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

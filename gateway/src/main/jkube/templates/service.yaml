apiVersion: v1
kind: Service
metadata:
  name: {{ include "jkube.fullname" . }}
  labels:
    {{- include "jkube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jkube.selectorLabels" . | nindent 4 }}

import { HateoasTitlecasePipe } from './hateoas-titlecase.pipe';

describe('HateoasTitlecasePipe', () => {
  it('create an instance', () => {
    const pipe = new HateoasTitlecasePipe();
    expect(pipe).toBeTruthy();
  });
});

apiVersion: v1
kind: Service
metadata:
  name: {{ include "rtms.fullname" . }}
  labels:
    {{- include "rtms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rtms.selectorLabels" . | nindent 4 }}

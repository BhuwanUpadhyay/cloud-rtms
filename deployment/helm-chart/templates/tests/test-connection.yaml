apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rtms.fullname" . }}-test-connection"
  labels:
    {{- include "rtms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rtms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

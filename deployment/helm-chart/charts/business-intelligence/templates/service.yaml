apiVersion: v1
kind: Service
metadata:
  name: {{ include "business-intelligence.fullname" . }}
  labels:
    {{- include "business-intelligence.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "business-intelligence.selectorLabels" . | nindent 4 }}

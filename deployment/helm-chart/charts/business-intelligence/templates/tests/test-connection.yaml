apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "business-intelligence.fullname" . }}-test-connection"
  labels:
    {{- include "business-intelligence.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "business-intelligence.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
